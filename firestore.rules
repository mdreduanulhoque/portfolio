rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default rules: Anyone can read, only authenticated users can write
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
